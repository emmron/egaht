export default {
  // Build configuration
  build: {
    target: 'es2020',
    minify: true,
    sourcemap: true
  },
  
  // Dev server configuration
  server: {
    port: 3000,
    host: 'localhost'
  }
};
