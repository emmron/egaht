/**
 * Core server utilities for Eghact dev server
 */

export function createServer(options = {}) {
  return {
    // Server utilities can be added here
    // Currently using Fastify directly in index.js
  };
}